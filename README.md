simpleClientCassandra
=====================

Install Cassandra (OS X):
```
brew install cassandra
```

Run Cassandra:

```
cassandra
```

Run the program:

```
mvn clean package
java -cp target/my-app-1.0-SNAPSHOT.jar romp.App
```
